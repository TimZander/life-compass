# Your personal answers — kept private by default so the repo stays publishable.
# Do your writing in here (copy a day file in, or keep free-form notes), and the
# blank worksheets under days/ stay clean. Remove these lines to version your answers.
my-compass.md
answers/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Jekyll / GitHub Pages local build output
_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock

# Generated: the question definitions, written into the client tier by build/schema.ts
# so Node, tsc and the browser all read one file. Rebuilt by the pre* npm hooks.
src/client/schema.ts

# Build output and tooling
dist/
node_modules/

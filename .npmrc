# Enforce the Node version in package.json's `engines`.
#
# The build runs TypeScript directly via Node's type stripping (Node 22.18+). A version
# check written inside the build could never fire: on an older Node the file fails to
# parse before any of its code runs. Install time is the only place the requirement can
# be stated where it will actually be read.
engine-strict=true
